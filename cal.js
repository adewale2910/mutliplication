function num(par) {
    show.innerText  += par
     }
    

     function equ() {
      show.innerText = eval( show.innerText)
      
     }
     function AC( ) {
      show.innerText = " "
     }
     function del() {
      show.innerText = show.innerText.slice(0, -1)
     }